use p3_field::{Field, FieldAlgebra};
use zkm_derive::AlignedBorrow;
use zkm_stark::ZKMAirBuilder;
pub const AES_SBOX: [u8; 256] = [
    ,
];

#[derive(AlignedBorrow, Default, Debug, Clone, Copy)]
#[repr(C)]
pub struct SubsByte<T> {
    pub positions: [[T; 32]; 4],
    // if this byte is in range [0..=127]
    pub is_left: T,
    // the substituted byte
    pub value: T,
}

impl<F: Field> SubsByte<F> {
    pub fn populate(&mut self, byte: u8) -> u8 {
        let mut pos = byte;
        if byte > 127 {
            self.is_left = F::ZERO;
            pos -= 128;
        } else {
            self.is_left = F::ONE;
        }

        for i in 0..128 {
            let row = i / 32;
            let col = i % 32;
            if i as u8 == pos {
                self.positions[row][col] = F::ONE;
            } else {
                self.positions[row][col] = F::ZERO;
            }
        }

        let substituted = AES_SBOX[byte as usize];
        self.value = F::from_canonical_u8(substituted);
        substituted
    }

    pub fn eval<AB: ZKMAirBuilder>(
        builder: &mut AB,
        cols: SubsByte<AB::Var>,
        byte: AB::Var,
        is_real: AB::Expr,
    ) {
        builder.assert_bool(cols.is_left);
        builder.assert_bool(is_real.clone());
        // if is_real = 0 then is_left must be 0
        builder.assert_eq((AB::Expr::ONE - is_real.clone()) * cols.is_left, AB::Expr::ZERO);

        // exactly one position is selected
        let sum_positions = cols
            .positions
            .iter()
            .map(|&pos| pos.iter().map(|&p| p.into()).sum::<AB::Expr>())
            .sum::<AB::Expr>();
        builder.assert_eq(sum_positions, is_real.clone());

        for i in 0..128 {
            // positions are boolean
            let row = i / 32;
            let col = i % 32;
            builder.assert_bool(cols.positions[row][col]);
            builder.assert_eq(
                (AB::Expr::ONE - is_real.clone()) * cols.positions[row][col],
                AB::Expr::ZERO,
            );
            // if is_left = 1 then byte = i else byte = i+128
            builder.assert_eq(
                cols.is_left
                    * (AB::Expr::from_canonical_usize(i) - byte)
                    * cols.positions[row][col],
                AB::Expr::ZERO,
            );
            builder.assert_eq(
                (AB::Expr::ONE - cols.is_left)
                    * (AB::Expr::from_canonical_usize(i + 128) - byte)
                    * cols.positions[row][col],
                AB::Expr::ZERO,
            );

            // value = SBOX[byte]
            builder.assert_eq(
                cols.is_left
                    * (AB::Expr::from_canonical_u8(AES_SBOX[i]) - cols.value)
                    * cols.positions[row][col],
                AB::Expr::ZERO,
            );
            builder.assert_eq(
                (AB::Expr::ONE - cols.is_left)
                    * (AB::Expr::from_canonical_u8(AES_SBOX[i + 128]) - cols.value)
                    * cols.positions[row][col],
                AB::Expr::ZERO,
            );
        }
    }
}
